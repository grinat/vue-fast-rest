/**
 * @typedef {Object} services
 * @property {DefaultRestClient} restClient
 * @property {Vuex.Store} store
 * @property {Object} services
 * @property {PluginConfig} config
 */
export const services = {
  restClient: {},
  store: {},
  services: {},
  config: {}
}
